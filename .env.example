# v0.1 works without API keys because mock mode is enabled by default.
USE_MOCK_DATA=true

# Add later, then set USE_MOCK_DATA=false after the real adapters are completed.
ATTENTION_INSIGHT_API_KEY=
OPENAI_API_KEY=
